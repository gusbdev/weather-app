import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginTop: 20,
    marginBottom: 20,
    padding: 10,
    backgroundColor: "#333",
    borderColor: "#fff",
    opacity: 0.7,
    borderRadius: 20,
  },
  textStyle: {
    textAlign: "center",
    fontSize: 18,
    color: "white",
  },
});
