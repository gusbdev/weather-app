import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
  },
  paragraph: {
    margin: 10,
    fontSize: 18,
    fontWeight: "bold",
    textAlign: "center",
    color: "#fff",
  },
  card: {
    display: "flex",
    backgroundColor: "#333",
    opacity: 0.7,
    borderRadius: 20,
  },
  cardSize: {
    display: "flex",
    width: 270,
  },
});
